<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="PDMI6TP02.Views.Principal">
    <ContentPage.Content>
        <StackLayout Padding="10">
            <Label 
                Text="Style Resources"
                FontSize="Small"
                FontAttributes="Bold"
                TextColor="Blue"                
                HorizontalOptions="Center" />

            <Button
                Text="ESTILO PADRÃO"
                Clicked="BtnEstiloPadrao"/>
            <Button 
                Text="ESTILO DINAMICO"
                Clicked="BtnEstiloDinamico"/>
            <Button 
                Text="ESTILO EVENT TRIGGRES"
                Clicked="BtnTrigger"/>
            <Button 
                Text="ESTILO GLOBAL" />

            <Label 
                Text="Passar parametro"
                FontSize="Small"
                FontAttributes="Bold"
                TextColor="Blue"                
                HorizontalOptions="Center" />

            <Button Text="HOME" 
                Clicked="BtnHome"/>
            <Button Text="MESSAGING CENTER" />

            <Label 
            Text="ListView"
            FontSize="Small"
            FontAttributes="Bold"
            TextColor="Blue"                
            HorizontalOptions="Center"/>

            <Button Text="SIMPLES" 
                Clicked="BtnListViewSimples" />
            <Button Text="CLASSE COMPLEXA" />
            <Button Text="PRODUTO"
            Clicked="BtnProduto"/>

        </StackLayout>
    </ContentPage.Content>
</ContentPage>